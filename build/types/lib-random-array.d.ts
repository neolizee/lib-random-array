export declare class UseRandomArray {
    number(length?: number, min?: number, max?: number, unique?: boolean): number[];
    string(length?: number, character?: number): string[];
    boolean(length?: number): boolean[];
}
